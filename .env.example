# Backend API URL (no trailing slash)
# Local: use empty or http://localhost:5000 (dev server can proxy /api)
# Production: set to your API URL, e.g. https://api.yourdomain.com
VITE_API_URL=http://localhost:5000
